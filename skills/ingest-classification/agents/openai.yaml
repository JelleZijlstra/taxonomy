interface:
  display_name: "Ingest Classification"
  short_description: "Transcribe source classifications into manifests"
  default_prompt:
    "Use $ingest-classification to transcribe a source classification into a validated
    recommendation manifest."
